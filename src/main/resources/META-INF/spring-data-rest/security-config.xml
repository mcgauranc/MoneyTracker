<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd">

    <security:global-method-security secured-annotations="enabled" pre-post-annotations="enabled"/>

    <!--<security:http auto-config="true" use-expressions="true" create-session="stateless"-->
    <!--entry-point-ref="digestEntryPoint">-->

    <security:http auto-config="true" use-expressions="true" create-session="stateless"
                   entry-point-ref="basicEntryPoint">
        <!--USER-->
        <security:intercept-url method="GET" pattern="/users/**" access="isAuthenticated() and (hasRole('ROLE_USER'))"/>
        <security:intercept-url method="GET" pattern="/account/**"
                                access="isAuthenticated() and (hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="GET" pattern="/accountType/**"
                                access="isAuthenticated() and (hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="GET" pattern="/address/**"
                                access="isAuthenticated() and (hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="GET" pattern="/authorities/**"
                                access="isAuthenticated() and (hasRole('ROLE_ADMIN'))"/>

        <security:http-basic entry-point-ref="basicEntryPoint"/>
        <!--<security:custom-filter ref="digestFilter" after="BASIC_AUTH_FILTER"/>-->
    </security:http>

    <security:authentication-manager alias="authenticationManager">
        <security:authentication-provider user-service-ref="moneyUserDetailsService">
            <security:password-encoder hash="md5">
                <security:salt-source user-property="username"/>
            </security:password-encoder>
        </security:authentication-provider>
    </security:authentication-manager>

    <bean id="moneyUserDetailsService" class="com.wraith.security.MoneyUserDetailsService">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!--<bean id="digestFilter" class="org.springframework.security.web.authentication.www.DigestAuthenticationFilter">-->
    <!--<property name="userDetailsService" ref="moneyUserDetailsService"/>-->
    <!--<property name="authenticationEntryPoint" ref="digestEntryPoint"/>-->
    <!--</bean>-->

    <bean id="basicEntryPoint" class=
            "org.springframework.security.web.authentication.www.BasicAuthenticationEntryPoint">
        <property name="realmName" value="MoneyTracker Realm via Basic Authentication"/>
    </bean>

    <!--<bean id="digestEntryPoint" class=-->
    <!--"org.springframework.security.web.authentication.www.DigestAuthenticationEntryPoint">-->
    <!--<property name="realmName" value="MoneyTracker Realm via Digest Authentication"/>-->
    <!--<property name="key" value="acegi"/>-->
    <!--</bean>-->

</beans>