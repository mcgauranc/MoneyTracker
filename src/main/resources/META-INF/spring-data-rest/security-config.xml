<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd">

    <!--<security:global-method-security secured-annotations="enabled" pre-post-annotations="enabled"/>-->

    <security:http auto-config="true" use-expressions="true" create-session="stateless">
        <!--USER-->
        <security:intercept-url method="GET" pattern="/user/**" access="(hasRole('ROLE_USER'))"/>
        <security:intercept-url method="POST" pattern="/user/**" access="(hasRole('ROLE_USER'))"/>

        <!--AUTHORITIES-->
        <security:intercept-url method="GET" pattern="/authorities/**" access="(hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="PUT" pattern="/authorities/**" access="(hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="POST" pattern="/authorities/**" access="(hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="DELETE" pattern="/authorities/**" access="(hasRole('ROLE_ADMIN'))"/>

        <!--GROUPS-->
        <security:intercept-url method="GET" pattern="/groups/**" access="(hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="PUT" pattern="/groups/**" access="(hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="POST" pattern="/groups/**" access="(hasRole('ROLE_ADMIN'))"/>
        <security:intercept-url method="DELETE" pattern="/groups/**" access="(hasRole('ROLE_ADMIN'))"/>

        <security:http-basic/>
    </security:http>

    <security:authentication-manager>
        <security:authentication-provider user-service-ref="moneyUserDetailsService">
            <security:password-encoder hash="md5">
                <security:salt-source user-property="username"/>
            </security:password-encoder>
        </security:authentication-provider>
    </security:authentication-manager>

    <bean id="moneyUserDetailsService" class="com.wraith.security.MoneyUserDetailsService">
        <property name="dataSource" ref="dataSource"/>
    </bean>

</beans>